name: Revisión de HTML 🚀

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar mi código
        uses: actions/checkout@v4

      - name: Revisar HTML con HTMLHint
        uses: htmlhint/htmlhint-action@v2
        with:
          pattern: 'index.html'