name: Revisión de HTML 🚀

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar mi código
        uses: actions/checkout@v4

      - name: Instalar validador
        run: npm install -g htmlhint

      - name: Ejecutar validación
        run: htmlhint index.html